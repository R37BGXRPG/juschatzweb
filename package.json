{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "deploy": "npm run build && git add dist -f && git commit -m \"Deploy to GH Pages popcorn🍿\" && git subtree push --prefix dist origin gh-pages && git reset --hard HEAD~1",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}